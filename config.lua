Config = {}

Config.anotherNotify = {
    enable = false,
    callback = function(type, content, duration)
        -- Support only (typy, content, duration parameters) !!

        -- Code Target Notify Here!!
    end
}